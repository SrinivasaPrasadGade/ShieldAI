# ── ShieldAI Base Dependencies ──────────────────────────────
# Shared by all services (API, worker, realtime)

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.0
pydantic>=2.0.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.1

# Firebase
firebase-admin>=6.5.0

# AI (Gemini — primary engine)
google-genai>=0.6.0

# Logging
structlog>=24.1.0

# Storage & Cache
redis>=5.0.0

# Graph Analysis
networkx>=3.0
python-louvain>=0.16
